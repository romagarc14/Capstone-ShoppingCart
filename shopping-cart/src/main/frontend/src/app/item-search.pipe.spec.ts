import { ItemSearchPipe } from './item-search.pipe';

describe('ItemSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
